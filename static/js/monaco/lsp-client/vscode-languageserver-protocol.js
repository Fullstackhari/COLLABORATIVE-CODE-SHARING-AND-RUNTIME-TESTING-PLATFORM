export const RequestType = class {
    constructor(method) {
        this.method = method;
    }
};

export const NotificationType = class {
    constructor(method) {
        this.method = method;
    }
};
